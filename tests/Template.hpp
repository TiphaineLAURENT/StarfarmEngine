//
// Created by Tiphaine LAURENT
//

#ifndef STARFARMENGINE__HPP
# define STARFARMENGINE__HPP

# include <ostream>

namespace star
{
        class 
        {
                // ATTRIBUTES
        private:
        public:

                // METHODS
        public:// CONSTRUCTORS
                () = default;
                ~() = default;
                (const  &copy) = delete;
                (&&) = delete;

        public: //OPERATORS
                 &operator=(const  &other) = delete;
                 &operator=(&&) = delete;

        public:
        private:
  };

        ::std::ostream &operator<<(::std::ostream &out, const &);
}

#endif /* !STARFARMENGINE__HPP */
